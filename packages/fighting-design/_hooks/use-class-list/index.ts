// const PREFIX = 'f-' as const
// const STATE = 'is-' as const

/**
 * BEM 明明规范
 * https://github.com/FightingDesign/fighting-design/issues/178
 * https://getbem.com/naming/
 * https://getbem.com/
 * https://developer.mozilla.org/en-US/docs/Learn/CSS/Building_blocks/Organizing#other_tools_that_can_help
 * 
 * @param name 
 */
// export const useClassList = (name: string) => {
//   const _name: string = PREFIX + name

//   const n = (target: string): string => _name + target

//   const is = (target: string): string => STATE + target

//   return { n, is }
// }
