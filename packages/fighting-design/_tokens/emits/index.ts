/**
 * emit 回调方法名称
 *
 * 主要用于基础的数据同步
 */
export const EMIT_UPDATE = 'update:modelValue' as const

/**
 * emit 回调方法名称
 *
 * 主要用于控制显示隐藏
 */
export const EMIT_VISIBLE = 'update:visible' as const

/**
 * emit 回调方法名称
 *
 * 用于更新日期
 */
export const EMIT_DATE = 'update:date' as const

/**
 * emit 回调方法名称
 *
 * 用于更新文件列表
 */
export const EMIT_FILES = 'update:files' as const

/**
 * emit 回调方法名称
 *
 * 用于更新时间
 */
export const EMIT_TIME = 'update:time' as const
