{
  "name": "start",
  "scripts": {
    "dev": "vite",
    "build": "rimraf dist && vue-tsc --noEmit && vite build"
  },
  "devDependencies": {
    "vconsole": "^3.14.6"
  },
  "dependencies": {
    "markdown-it": "^13.0.1",
    "markdown-it-container": "^3.0.0"
  }
}
